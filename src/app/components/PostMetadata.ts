export interface PostMetadata {
    title: string;
    date: string;
    subtitle: string;
    slug: string;
  }

export interface WorkMetadata {
  title: string;
  subtitle: string;
  img: string;
  link: string;
}